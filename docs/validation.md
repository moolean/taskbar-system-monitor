# 3.7.0 验证记录

## 2026-09-18 独立常驻、自启与生命周期脚本重构

- 安装目录和数据目录统一为 `%USERPROFILE%\.local\TaskbarSystemMonitor`。本机旧安装位于打包应用的 AppData 虚拟视图，计划任务无法访问原逻辑路径（结果 `0x80070002`）；迁移至非虚拟目录后，计划任务直接启动正式 EXE，父进程为 Windows `svchost.exe`，安装 / 启动脚本退出后应用继续运行。
- 使用当前用户登录触发、交互式令牌、最低权限的计划任务：登录后延迟 5 秒、无执行时限 `PT0S`、允许电池供电、不依赖网络或空闲状态、忽略重复实例、异常失败最多重试 3 次。任务注册成功后移除旧 Run 注册表项；正常退出不通过常驻脚本强行拉起。
- 启动任务的注册、查询和删除由 `Startup` 统一管理，应用设置和脚本复用同一实现。数据路径 / 单次旧配置迁移由 `AppPaths` 管理；PowerShell 入口复用 `MonitorTools`，分别负责 build、test、install、start、stop、status、uninstall。构建和自检在停止正式安装前完成，升级保留上版 EXE，退出走保存笔记与释放 AppBar 的正常流程。
- 自动验证通过：全部 11 个 PowerShell 脚本 / 模块语法、构建与安装路径分离；任务 XML 的权限、登录触发、无限运行、电池 / 网络 / 空闲策略、失败重试、路径转义、用户隔离、外来任务拒绝；旧配置逐字节复制、保留源文件、重复迁移不覆盖。既有关键词、拖动排序、主题对比度、额度解析及布局回归通过。
- 桌面回归通过：非置顶、Codex / IP 被动文本、右键仅菜单、关键词直达与双向拖动、拖动阈值 / 区域外取消 / 刷新冻结 / 陈旧索引取消、最大化不重叠、50 次无漂移定位、全屏恢复、隐藏 / 显示 / 退出空间恢复、紧凑笔记聚焦与动画、失焦收起时保存草稿。使用测试窗口与隔离数据，不编辑真实工作项。
- 正式安装 3.7.0.0 与最终构建 SHA-256 一致。真实配置与保留的旧配置 SHA-256 完全一致；正常停止后通过计划任务重新启动，只有一个正式进程；重复 start 保持同一 PID；关闭再打开登录自启保持当前 PID、任务运行和配置不变。最终自启开启，任务状态 Running，旧 Run 项已移除。
- 测试中一次短时状态助手出现原生 Runtime Library 资源错误，关闭该助手后恢复；维护命令已移到 GUI 初始化之前，并为超时助手增加有界清理。后续安装、状态检查、停止 / 启动与自启切换均通过，未强制结束正式笔记进程。
- 验证边界：未实际重启电脑、注销登录或关闭 Codex，也未完成 24 小时老化与真实异常崩溃重启实验；独立性通过系统服务父进程、独立文件路径和启动脚本退出后的存活验证，长时间运行 / 重启策略通过任务配置和自动测试验证。关机 / 注销会结束当前桌面会话，后续登录才启动；这不是未登录也显示 UI 的系统服务。

以下为历史版本验证记录，其中“未推送”描述的是对应版本当时的状态。

## 3.6.0 历史验证

## 2026-09-17 关键词拖动排序与灰紫色 WORK 标签

- 横条直接按住关键词左右拖动，达到系统拖动阈值后显示插入线；松开提交一次顺序变更，拖离工作区域取消，不触发普通点击。捕获鼠标期间冻结布局快照，采样刷新延后应用；并发新增 / 删除 / 重命名使旧索引失效时取消落点。仅移动工作项，不拖动整个模块，不写入无关设置。已打开笔记先保存草稿并保持同一笔记选中；失败时保留草稿，不静默丢失内容。
- 修复列表拖放：补齐 DragEnter，使用行中点决定前 / 后插入位置，增加六点抓手和插入线，边缘支持滚动。拖放结束后抑制尾随的 MouseClick，保持列表而非误打开笔记；拖动中的临时焦点变化不自动收起面板。过滤时不排序，列表标题明确提示清空搜索。
- WORK 改为独立低饱和灰紫色圆角标签、细描边和同色文字，不改变字号、栏高、标签宽度及数量折叠规则；悬停仅突出当前标签。浅 / 深背景下文字与常态 / 悬停底色的对比度自动验证不低于 4.5:1，高对比度模式采用系统颜色。
- 自动测试通过：2–30 项的所有源索引和插入槽组合，验证相对顺序及笔记 / 链接保留；非法落点、过滤中禁止移动、列表保持、打开笔记的未保存草稿保护、保存失败保留、临时 XML 保存并重载的顺序保持；颜色对比、只改变单个悬停标签的逐像素检查、区域外取消、冻结快照隔离。原有关键词、面板、迁移、额度、288 组关键词、12 组紧凑排版和 108 组横条布局回归通过。
- 桌面集成回归通过：点击阈值、正反向拖动事件、采样不打断、区域外取消、并发工作项变更取消、拖动不触发笔记点击、鼠标捕获释放；原有右键菜单、非置顶、最大化边界、50 次无漂移定位、隐藏 / 显示 / 退出空间恢复与自动保存测试通过。
- Computer Use 使用不写真实配置的示例：列表首项拖到末尾再拖回，保持列表、选中项和保存状态，随后单击仍打开原笔记；横条首项左右双向拖动，样例报告确认恰好 2 次排序。检查浅 / 深色标签及实际右键菜单。工具无法直接点击超出窄横条窗口边界的弹出菜单项，改用菜单键盘导航正常关闭样例。所有样例进程已退出，临时 AppBar 空间已释放。未用 UI 操作真实工作项；长列表边缘滚动、高对比度实际桌面和跨屏拖动未人工验证。
- 已安装并启动 3.6.0.0，安装 EXE 与最终构建 SHA-256 一致，升级前后真实配置 SHA-256 完全一致，只有一个正式监控进程。浅 / 深预览已重新渲染检查。未推送远端。

## 3.5.1 历史验证

## 2026-09-17 详情删除、右键菜单与英文横条

- 紧凑笔记详情右下角新增轻量“删除”，已有删除历史时显示“撤销”；使用浅 / 深色都可读的低饱和警示文字，不放大面板。删除后选择相邻项，最后一项删除后进入快速输入，列表底部仍可撤销。撤销保留最新未保存笔记、链接和原位置；沿用会话内历史与自动保存，未增加状态 / 进度字段。
- 横条右键仅打开随主题配色的 Work notes / Resource details / Settings… 菜单，不再触发设置；选择 Settings 才打开。左键末尾省略号保留设置快捷入口，中键不触发设置。Codex / IP 保持被动纯文本。
- 内置横条标签、提示、连接状态、电池状态与额度摘要使用英文；日期固定英文星期和公历，数值固定小数点。工作关键词原样显示，设置与笔记界面不翻译。渲染示意图仅含虚构关键词和保留给文档示例的 IP。
- 自动测试通过：详情直接删除、选择相邻项、最后一项删除、保存后的完整撤销、写入失败后撤销恢复、调用者配置隔离；zh-CN / fr-FR / ar-SA 三种区域下的日期、数值、标签与额度摘要；原有小面板、自动保存、迁移、288 组关键词、12 组紧凑布局、108 组横条组合回归。
- 真实桌面回归通过：右键 IP 和横条两端均只显示菜单；关闭菜单不触发设置，显式选择 Settings 才触发；左键省略号和中键路由符合预期；详情删除按钮可见、启用且位于紧凑面板内。保留非置顶、最大化边界、50 次定位无漂移、隐藏 / 显示 / 退出空间恢复、展开收起动画和自动保存回归。
- Computer Use 只使用不写入配置的示例窗口，检查浅 / 深色列表及详情中的删除按钮、紧凑尺寸、文字层级与配色，未通过 UI 删除任何真实或示例工作项。删除与撤销由内置自动测试验证；菜单事件由本窗口的桌面集成测试验证，未另做人工右键点击检查。
- 已安装并启动 3.5.1.0，安装 EXE 与最终构建 SHA-256 一致；升级前后真实配置文件 SHA-256 完全一致，仅有一个正式监控进程。示例窗口均正常关闭。未推送远端。

## 3.5.0 历史验证

## 2026-09-17 紧凑随手记面板与动画

- 独立工作窗口从 804 × 530 逻辑像素的双栏窗口改为默认约 420 × 242 的单栏面板。保留同一份编辑草稿，在列表与笔记间切换，链接按需展开；已有合法链接保留一键打开入口。笔记根据内容增高并设上限，列表最多展示约 6 行后滚动；不新增状态或进度字段。
- 外观采用统一的微软雅黑 UI、中性色、轻量工具栏、圆角选中行，移除外层嵌套边框和短笔记滚动条。搜索框增加输入提示，真实数据不用于示意图或示例检查。新增 UiButton / SettingsCard 的轻量外观标记默认为关闭，不改变设置页的既有布局。
- 150 ms 开启 / 高度调整与 110 ms 收起使用 UI Timer 和单调缓出曲线，不阻塞输入线程。遵循 [Windows 客户区动画设置](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-systemparametersinfow)，高对比度 / 远程桌面也直接切换，不改变系统设置。搜索提示使用 [原生单行编辑提示](https://learn.microsoft.com/en-us/windows/win32/controls/em-setcuebanner)。
- 默认失去焦点前先保存再收起；“固定”只保持本次面板展开，不置顶、不写入配置。保存失败或未完成输入保留面板与草稿。程序关闭和进入设置仍同步关闭，不受收起动画延迟。拖动标题可移动；实机拖动时发现可越过屏幕边缘，已增加松开后的工作区边界约束。
- 自动测试通过：短 / 长笔记动态高度、列表滚动上限、切换保留草稿、链接按需展开、保存失败不启动淡出、36 组屏幕边界组合、动画首尾与单调曲线，以及原有采样、保存、迁移、288 组关键词和 108 组横条布局回归。真实桌面回归验证 420 逻辑像素面板、直达笔记焦点、链接 / 列表 / 收起动画完成状态、非置顶、失去焦点时将未等满 650 ms 的草稿写入恰好一次后关闭，所有数据均为虚构样例。
- Computer Use 实机检查浅 / 深色列表和笔记视图，检查轻按钮、输入提示、短笔记无滚动条；用虚构关键词完成输入 → Enter → 自动进入笔记 → 输入 → 保存状态 → 拖动 → Esc 收起。上一轮已验证链接展开后的输入焦点；本轮未访问任何示例 URL，没有编辑真实工作项。为便于工具识别，示例窗口在任务栏可见且默认固定；正式面板不在任务栏增加按钮。
- 已安装并启动 3.5.0.0，安装 EXE 与最终构建 SHA-256 一致；升级前后实际配置文件 SHA-256 完全一致，现有关键词、笔记、链接和设置完整保留。所有示例窗口已关闭。未手工测试多屏间 DPI 切换、真实 IME 长文编辑、关闭系统动画的分支或独占全屏；未测项目不记作通过。源码尚未提交或推送。

## 3.4.2 历史验证

## 2026-09-17 Codex / IP 纯信息显示

- 横条上的 Codex、IP 改为只读文本：无按钮底色、无悬停高亮、默认箭头光标；左键不触发模块详情，也不会回落打开资源面板。全局右键设置、CPU 详情和工作关键词交互不变，额度 / IP 自动刷新、设置页和命令行详情入口保留。
- 浅 / 深两种配色下，渲染测试验证静态背景无色块，悬停前后图像逐像素一致。真实桌面上的内置回归使用虚构快照调用本窗口事件处理器，验证 Codex / IP 左键不产生任何打开事件、光标为默认箭头，同时 CPU、关键词和右键设置事件仍正常；没有自动点击或修改用户工作数据。
- 原有自检、关键词宽度边界与桌面布局回归全部通过。示例预览重新渲染并检查；已安装运行 3.4.2.0，安装 EXE 与构建 SHA-256 一致，升级前后真实配置文件 SHA-256 一致。此轮没有重新进行人工鼠标点击检查，源码尚未提交或推送。

## 3.4.1 历史验证

## 2026-09-17 关键词按宽度折叠与紧凑横条

- 修复 `WorkTargets` 中最多显示 3 个关键词的硬限制，改为按文本实测宽度决定数量。先检查全部内容是否放得下，仅在确实超出后才预留 `+N`；精确放满时不提前折叠。保留原列表顺序和点击索引，`+` 始终紧随可见项。
- 模块内边距由 8 改为 5 逻辑像素，标签间隔由 6 改为 4，关键词间隔由 4 改为 3；不修改用户字号或栏高。Codex / IP 按内容定宽，不再分配拉伸余量，工作关键词使用全部剩余宽度。关闭“利用整行宽度”时，也按工作内容实际需求扩展，不沿用固定 230 宽度。工作背景仅覆盖实际内容，不画成长空白色块。
- 288 组关键词布局回归通过：4 种 DPI × 3 种字号 × 4 种宽度 × 6 种数量（0 / 1 / 3 / 5 / 10 / 30）。验证足够宽时显示全部、精确放满时没有 `+N`、溢出数量准确、索引与顺序保持、无重叠、紧邻间距和新建入口。12 组整体布局验证默认模块在 1024 逻辑像素可用、宽屏不拉伸非工作模块、余量全部给工作区，以及非铺满模式仍可显示超过 3 项。
- 自检、原有 108 组横条布局和真实桌面回归均通过：非置顶、最大化不重叠、50 次定位、模拟全屏恢复、改高度、隐藏 / 显示 / 退出恢复，以及关键词直达焦点。浅 / 深预览用 10 个虚构关键词重新渲染并检查，未使用真实工作内容生成预览。
- 已安装运行 3.4.1.0，安装文件与最后构建 SHA-256 一致。升级前后真实配置文件 SHA-256 完全一致，4 个现有工作项和全部设置保留；仍非置顶、公网查询关闭。此轮未操作真实工作项，未重新进行完整的人工点击测试；交互回归由自动测试覆盖。源码尚未提交或推送。

以下为历史版本验证记录。

## 3.4.0 历史验证

## 2026-09-17 快捷交互与自动保存

- 横条按关键词独立命中，点击直接选择对应项并把光标放入笔记；`+N` 打开列表，`+` 进入快速输入。新增入口紧随已有关键词，空列表时紧邻“工作”标签，避免宽屏下额外的鼠标移动。关键词窗口缩小为 804 × 530 逻辑像素，在点击位置附近、工作区底部展开。
- 搜索与新建合并为一个输入框：Enter 直接创建并进入笔记，已有同名项则直接选择，不重复添加。650 ms 防抖自动保存；Esc / 关闭 / 进入设置 / 正常退出前立即写入，无需普通保存确认。失败保留草稿并阻止关闭，提供重试；空关键词重命名不静默丢弃原项，未完成的链接可以作为文字保存但不能执行。
- 支持拖动排序、方向按钮与列表 Alt+方向键；过滤期间禁用排序。移除一次生效，撤销可恢复完整内容和位置；应用会话内保留最多 30 条移除记录，关闭工作窗口不清除，退出应用后不保留。设置页仍使用统一“保存更改”，取消设置不会提交草稿。
- 自动测试通过：直接输入、同名定位、30 项上限、草稿隔离、删除撤销及重新打开后的撤销、无效链接不可执行、失败后草稿保留、输入合并为一次定时保存、关闭前立即写入临时 XML、笔记缩进和空行保留。60 组关键词布局验证目标位置 / 索引、无重叠、入口可达及相邻间距；原有 108 组横条布局、额度解析、迁移和安全回归通过。
- Computer Use 在真实 125% DPI 桌面上，使用不写真实配置的示例窗口完成：初始输入框聚焦 → 输入关键词 → Enter 新建并聚焦笔记 → 输入笔记 → 拖到首位 → 显示已保存 → Esc 关闭。未点击示例链接，未修改真实工作项。删除撤销由自动测试验证，未通过 UI 操作用户数据。
- 真实桌面回归通过：非置顶样式、最大化边界、50 次定位无漂移、置顶切换及模拟全屏恢复、改高度、隐藏 / 再显示 / 退出恢复工作区。新增窗口检查验证 Show 之后的消息处理不改变直达关键词的选择与笔记焦点，窗口非置顶且无修改时直接关闭。
- 已安装运行 3.4.0.0，安装 EXE 与最后构建的 SHA-256 一致；升级前后真实配置文件 SHA-256 一致。非置顶与公网查询关闭保持不变，实际工作项仍为 0。示意图重新渲染，仅包含虚构数据。源码尚未提交或推送。
- 本轮未实测真实中文 IME 组合输入、工作窗口跨屏 DPI、真实独占全屏应用；强制结束程序不保证保存最后 650 ms 内的草稿。深色工作窗口沿用上一版已验证的主题体系，本轮重新渲染浅 / 深横条示意图，未再次实测深色窗口。

以下为历史版本验证记录；旧版手动保存与关闭确认流程已由 3.4.0 的自动保存替代。

## 3.3.0 历史验证

## 2026-09-17 关键词工作追踪

- 移除工作项状态和进度，模型改为关键词 + 可选笔记 / 链接。旧 title 兼容读取并定向迁移为 keyword，移除旧 status / progress，保留笔记、链接、顺序和其他配置；原完成项不再隐藏。
- 工作窗口重做为关键词列表与详情编辑双栏，支持搜索、新建、排序、移除、就地编辑和统一保存 / 撤销。设置页复用同一编辑器。窗口已打开时再次点击仅激活，不重载草稿；定时采样不触碰工作草稿。切换到设置前先走关闭确认，避免两份编辑器互相覆盖。
- 自动回归覆盖旧字段迁移（包括非法旧进度值）、迁移幂等、未知配置保留、纯关键词摘要、关键词必填 / 详情可空、链接限制、草稿与原数据隔离、切换项保留草稿、搜索和排序、无结果新增、30 项上限、移除 / 撤销、空列表、保存成功及失败草稿保留。损坏 XML 不覆盖。
- Computer Use 检查了真实 125% DPI 下的浅色、深色独立工作窗口，以及设置中的工作追踪页。通过不写真实配置的示例模式完成新建仅关键词 → 保存 → 关闭；未访问示例链接。检查中发现 LF 笔记显示为一行，已在显示层转换为 CRLF，深色窗口复查正确分行。
- 构建、自检和 108 组横条布局组合通过。真实桌面非置顶、最大化边界、50 次定位、置顶切换 / 模拟全屏恢复、调整高度、隐藏 / 再显示 / 退出释放工作区回归通过。关键词窗口跨屏 DPI 与系统强制结束后的草稿恢复不在本次验证范围。
- 已安装运行 3.3.0.0，安装文件与构建 SHA-256 一致；非置顶和公网查询关闭设置保留。实际配置工作项为 0，文件最后写入时间早于本次升级，确认不是升级清空了工作项。迁移核验脚本汇总输出时发生 PowerShell 枚举方法错误，修正后完成字段与版本核验；不将原本为空的实际工作项记作真实存量数据迁移验证，数据保留由自动测试覆盖。
- 示例检查未写入真实工作项；安装后打开真实工作窗口，检查空列表和新建入口，未编辑用户数据。源码尚未提交或推送。

以下为 3.2.0 及更早的历史验证记录。

## 2026-09-16 移除日历

- 移除日历显示项、设置表单、会议详情、CalDAV 请求与解析、密码加密代码和日历诊断入口；可选公网 IP 查询的 HTTPS 客户端独立保留，仍然默认关闭。
- 新增迁移测试：旧账号和加密密码节点被清除，模块列表过滤 Calendar；所有其他 XML 节点、未知配置、模块顺序、工作笔记、字号、色调、Codex 路径、非置顶和公网查询开关完整保留。重复清理不重写文件，缺失配置无需处理，损坏 XML 不覆盖；2.x 和仅启用日历的旧配置都有可用回退。
- Windows 自带编译器构建、自检和 108 组 DPI / 排版测试通过。额度解析、配置往返、链接限制、同源 HTTPS 跳转保护、.NET 4.8 系统默认 TLS 与证书验证检查通过。此轮不请求飞书或公网位置服务。
- 真实 Explorer 桌面回归通过：默认非置顶、最大化窗口边界、连续 50 次定位无漂移、置顶开关与模拟全屏通知后的恢复、调整高度、隐藏 / 再显示 / 退出释放工作区。屏幕为 2560 × 1440，DPI 120；基线工作区高度 1380，24 逻辑像素栏预留后为 1350，退出恢复 1380。
- 已安装并启动 3.2.0.0，安装文件 SHA-256 与本次构建一致；实际用户配置已无 calendar 节点和 Calendar 模块。升级前剔除日历数据后的 XML 与升级后 XML 校验值一致，确认所有非日历设置未变。
- 使用 Computer Use 检查真实设置窗口：常规页仍为非置顶，数据连接页仅保留 Codex 与网络卡片，旧日历表单和占位空白已消失。尝试取消关闭窗口时，用户按 Escape 停止了 Computer Use；未继续操作，也未保存设置更改。预览图重新从虚构示例数据渲染，不包含真实工作项或网络地址。
- 不删除飞书服务端日程，不撤销飞书端专用密码；用户可按需自行撤销不再使用的凭据。尚未实测的桌面场景仍见文末。

以下为历史版本记录；其中日历测试和使用说明不适用于 3.2.0。

## 3.1.1 历史验证

## 2026-09-15 TLS 初始化修正

- 用户反馈“发现账户 / PROPFIND：网络连接失败”，截图本身没有具体 WebExceptionStatus，因此不能据此认定密码错误或唯一根因。
- 检查 3.1 发布 EXE，确认缺少 `TargetFrameworkAttribute`，尽管项目文件声明了 net48。直接 csc 编译没有生成 SDK 通常生成的目标版本标记，可能启用旧 TLS 兼容默认值。
- 同机系统网络工具确认飞书域名 DNS 可解析，HTTPS 无凭据请求返回 HTTP 401；这证明系统网络能连到认证层，不证明用户密码有效。
- 3.1.1 为两种构建路径统一加入 .NET Framework 4.8 目标属性。新增自检已通过：实际运行目标为 v4.8，`ServicePointManager.SecurityProtocol` 为 `SystemDefault`，证书验证回调未被替换。
- TLS 握手、证书信任、DNS、代理解析、连接失败及超时使用分开的安全错误说明，不记录认证头或密码。
- 单独的临时网络诊断 EXE 被系统拒绝启动且随后不可见；没有尝试关闭安全软件或绕过限制，临时诊断源码已移除。未获得该程序的 TLS 对比结果。
- 本轮构建没有重启用户填有尚未保存凭据的设置窗口；真实账户端到端同步需要保存后再升级验证。

## 2026-09-15 更新

- 已安装侧栏 + 分组卡片的新设置界面。检查常规、外观、工作追踪、数据连接页；底部预览 / 保存 / 取消固定，内容独立滚动。空凭据的“测试连接”会在卡片内显示提示，没有发送网络请求。
- 真实桌面回归确认默认不存在 `WS_EX_TOPMOST`；可选置顶开关生效，模拟全屏通知会让位，关闭置顶后再经历全屏 / 重新定位仍保持非置顶。最大化窗口、50 次定位无漂移、隐藏 / 显示 / 退出恢复检查全部通过。模拟通知不等同于全屏游戏实测。
- 新增模拟 CalDAV 服务测试，覆盖 principal → 相对 home → 相对 calendar → REPORT；响应内 HTTP 403 不作为空日程成功，错误显示请求阶段，裸域名自动补齐 HTTPS。
- 同源路径跳转保留请求方法和认证，先关闭上一响应再请求下一跳；跨源跳转仍被阻止。这一兼容分支尚未对真实飞书租户完成验证。
- 本次诊断读取到的实际配置仍为不含 `calendar` 节点的旧配置；因此**没有验证用户所述密码是否正确，也未复现真实服务器返回的同步失败**。需要用户在新连接页填入专用账户，点测试后提供具体阶段 / 错误码，不能声称日历已接通。
- 构建改为原子替换输出；安装在保留旧版备份后复制并校验 SHA-256，避开当前 MSIX 虚拟化目录的替换失败。公网位置仍未启用。

以下为 3.0 的验证记录，继续作为原有功能基线。

日期：2026-09-14。以下区分自动测试、真实桌面测试与尚未覆盖的场景。

## 信息工作台新增验证

- 已实测 C# 程序通过私有 `codex app-server --stdio` 读取官方只读额度接口：主 Codex 桶返回周剩余 90%，详情也包含服务端返回的 Spark 窗口及重置时间。该数值仅为测试时刻的快照。
- 额度测试覆盖 `rateLimitsByLimitId` 优先级、已用比例转换、缺失窗口不显示为零占用。
- 工作项标题、状态、进度、笔记、链接完成 XML 往返和深拷贝隔离测试；CalDAV 专用密码完成 DPAPI 往返与无明文落盘检查。
- 日历解析覆盖折行、转义、UTC / Asia/Shanghai、取消日程、DURATION 出现在 DTSTART 之前；未展开的重复日程明确报错。跨域日历地址不转发凭据，详情链接不接受文件协议和内嵌账户信息。
- 2048 逻辑像素下默认模块全部显示，日程 / 工作区吸收剩余宽度；浅色和深色预览使用保留示例 IP、虚构会议及工作项，不包含真实私人数据。
- 真实 125% DPI 桌面检查了三个设置页、预览、保存 / 取消按钮及工作项编辑窗口。修复嵌套选项卡重复缩放造成的底部裁切；临时工作项输入已取消，没有写入实际配置。
- Codex 详情的实际数据源测试已通过；详情弹窗未被当前桌面检查工具列出，因此不将其截图外观记为已通过的人工视觉检查。
- 公网位置查询默认关闭；安装前确认实际配置没有启用它，没有向 IP 查询服务发送请求。
- 飞书未填写真实 CalDAV 账户，公网定位未获得开启授权；这两个外部连接均**未完成端到端验证**，界面明确显示待连接 / 关闭状态。

## 普通测试

Windows 自带 C# 编译器构建成功，`--self-test` / `test.ps1` 返回 0。

- CPU 首次采样、正常计算、无时间增量、计数回退。
- 内存总量有效、占用比例在 0–100% 范围。
- 网速计数差分、零时间、计数回退、休眠式长间隔。
- 设置首次写入、原子替换、重新加载，含内容列表、字号、高度、主题、对齐、刷新间隔和首次运行标记。
- 无效设置值归一化、历史采样保留最近 120 项。
- 108 组布局：4 个 DPI（96 / 120 / 144 / 192）× 3 个字号 × 3 种对齐 × 3 种可用宽度。每组均绘制浅色 / 深色、验证项目边界不交叠且不侵入设置按钮。

`docs/taskbar-preview.png` 是代码渲染的示例数据，不是实时桌面截图。

## 真实桌面 AppBar 测试

在真实用户会话的 Explorer 桌面运行 `--appbar-test`，而非沙箱账户。主屏幕 2560 × 1440、DPI 120（125%）。

| 检查 | 结果 |
| --- | --- |
| 原始工作区 | `(0, 0, 2560, 1380)` |
| 默认 24 逻辑像素栏 | `(0, 1350, 2560, 30)`，工作区底部缩至 1350 |
| 最大化测试窗口 | 客户区未进入资源栏 |
| 连续定位 50 次 | 位置和预留高度无漂移 |
| 调整到 32 逻辑像素 | 栏变为 `(0, 1340, 2560, 40)` |
| 隐藏 | 工作区恢复至原始 1380 高度 |
| 再次显示 | 只申请一次正确的空间 |
| Dispose / 退出 | 工作区完整恢复 |

第一轮实测发现隐藏后再次显示没有重新申请空间：旧 Bounds 与目标相同，错误地跳过了 `ABM_SETPOS`。已增加独立的预留状态判断，修正后完整回归返回 0。

## 安装流程与原有功能回归

- 2.0 阶段已打开实际面板和设置窗口，并在界面中勾选时间、隐藏资源栏，保存后 XML 正确包含 Clock 和 `dock=false`。
- 使用安装脚本优雅停止实例、重新构建、保留设置并启动。仅托盘启动时重复启动仍可唤起面板。
- 3.0 安装保留原有资源模块，迁移时追加 Codex、日程、IP、工作追踪、时间；工作项初始为空，不植入示例项目。
- 已登记当前用户开机启动项；**未实际注销或重启 Windows 测试登录启动**。

## 尚未实测的范围

Explorer 崩溃重启、全屏游戏、远程桌面重连、自动隐藏任务栏、跨屏热插拔与运行中修改系统 DPI，以及 Windows 10 的各类任务栏排列尚未覆盖。代码处理了部分系统通知，但不能据此宣称这些环境都通过验证。

当前设计固定主屏幕底部整行 AppBar，不是任务栏内部组件；这项布局边界不会用透明覆盖或裁剪 Explorer 来规避。
